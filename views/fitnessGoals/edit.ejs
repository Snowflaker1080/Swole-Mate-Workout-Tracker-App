<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.4/css/bulma.min.css" />

    <title>Edit User Fitness Goals</title>
  </head>

  <body>
    <section class="section">
      <div class="container">
        <h1>Edit Fitness Goal</h1>
        <form action="/fitnessGoals/<%= goal._id %>" method="POST">
          <label>Start Weight:</label>
          <input type="number" name="startWeightKg" value="<%= goal.startWeightKg %>" required />
          <label>Target Weight:</label>
          <input type="number" name="targetWeightKg" value="<%= goal.targetWeightKg %>" required />
          <button type="submit">Update Goal</button>
          <button type="submit">Update</button>
        </form>

        <form action="/fitnessGoals/<%= goal._id %>?_method=DELETE" method="POST">
          <button
            type="submit"
            onclick="return confirm('Are you sure you want to delete this goal?');"
          >
            Delete
          </button>
        </form>
      </div>
    </section>
  </body>
</html>
